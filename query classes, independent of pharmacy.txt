class BigpharmaSearch(Pharmacy.Query):
    ARGUMENTS = {
        "name": str,
        "phone": str,
        "start_date": str,
        "end_date": str,
        "drug": str  
    }
RETURNS = ("name", "phone")

class WhoCalled2(Pharmacy.Query):
    ARGUMENTS = {
        "phone": str
    }
RETURNS = ("name", "phone")

class DrugsForPatient(Pharmacy.Query):
    ARGUMENTS = {
        "name": str
    }
RETURNS = ("drug_id", "date", "dosage")

class DoctorsWithOldPatients(Pharmacy.Query):
    ARGUMENTS = {
    }
RETURNS = ("name")

class CountContractsThatEndBefore(Pharmacy.Query):
    ARGUMENTS = {
        "date": str
    }
RETURNS = ("count")

class CountContractsThatBeginAfter(Pharmacy.Query):
    ARGUMENTS = {
        "date": str
    }
RETURNS = ("count")